name: auto-weekday-deploy

on: push

jobs:
  auto-weekday-deploy:
    name: 'Auto Build & Deploy'
    runs-on: ubuntu-latest

    strategy:
      matrix:
        include:
          - region: kor
            s3Path: s3://kakaowebtoon-web-dev/kor
            domain: co.kr

    steps:
      - uses: actions/checkout@v2

      - name: sdafadsfasdf
        id: step
        run: |
          echo "::set-output name=env::$(echo preview)"

      - name: Send slack if completed
        if: ${{ success() && env != preview }}
        uses: ./.github/actions/slack-notify
        with:
          region: ${{ matrix.region }}
          env: dev
          status: success
          domain: ${{ matrix.domain }}

      - name: Send slack if completed
        if: ${{ failure() && env != preview }}
        uses: ./.github/actions/slack-notify
        with:
          region: ${{ matrix.region }}
          env: dev
          status: success
          domain: ${{ matrix.domain }}
